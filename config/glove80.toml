# Ivan's Glove80 runtime configuration for the current moergo-rmk firmware.
bluetooth_name = "WhiteG80 {slot}"
default_layer = 0
#
# This personal state is intentionally separate from the stock compiled
# defaults and is applied at runtime through RMK's native Rynk protocol:
#
#   just check
#   just diff
#   just apply
#   just show
#   just pull
#
# Keymap model:
# - A [[layer]]'s position in this file is its firmware layer slot (0-7).
# - `id` is a source label; `name` is persistent firmware state restored by
#   apply. Pull preserves existing IDs when possible.
# - `keys` is the 6x14 grid, row-major, whitespace-separated, one row per
#   line. Tokens are the same QMK-style names `keymap read`/`keymap set`
#   use (see `keymap find`); `--` is an unbound key (KC_NO) and marks the
#   four physical holes at r0c5, r0c8, r5c5, r5c8. `#` comments run to the
#   end of the line. Columns 0-6 are the left half (col 6 = left thumbs),
#   columns 7-13 the right half (col 7 = right thumbs).
# - A sparse layer may omit `keys` and carry `[[layer.key]]` entries instead;
#   every key it does not name stays transparent, and a bind's lighting scene
#   can sit right beside it (README: "Selector-addressed bindings").
#
# Apply semantics: changed key bindings are written with read-back verification
# but are not atomic across the whole keymap. Durable lighting scenes are
# replaced atomically, and the full managed runtime state is verified afterward.

# --- Layer 0: Base -----------------------------------------------------------
# The bottom row's old Home/End pair is now the instant layer pair: MO(1) for
# Lower and MO(5) for Paseo, live on key-down with no tap-hold window. Both
# layers keep their tap-hold entries (LT(1,...) and TD(0)) as well. Home and
# End themselves move to the same two positions on Lower; reaching Home there
# means entering Lower through an LT key, since MO(1) is that key.
[[layer]]
id = "base"
name = "Base"
keys = """
KC_F1    KC_F2    KC_F3   KC_F4    KC_F5    --       KC_ESC        KC_BSPC  --       KC_F6  KC_F7    KC_F8    KC_F9          KC_F10
KC_EQL   KC_1     KC_2    KC_3     KC_4     KC_5     TD(0)         TG(4)    KC_6     KC_7   KC_8     KC_9     KC_0           KC_MINS
KC_TAB   KC_Q     KC_W    KC_E     KC_R     KC_T     MO(2)         KC_LGUI  KC_Y     KC_U   KC_I     KC_O     KC_P           KC_BSLS
KC_LCTL  KC_A     KC_S    KC_D     KC_F     KC_G     KC_BSPC       KC_SPC   KC_H     KC_J   KC_K     KC_L     LT(1,KC_SCLN)  KC_QUOT
KC_LSFT  KC_Z     KC_X    KC_C     KC_V     KC_B     KC_LGUI       KC_ENT   KC_N     KC_M   KC_COMM  KC_DOT   KC_SLSH        KC_RSFT
KC_GRV   MO(1)    MO(5)   KC_LEFT  KC_RGHT  --       KC_LALT       KC_RGUI  --       KC_UP  KC_DOWN  KC_LBRC  KC_RBRC        TG(3)
"""

# --- Layer 1: Lower (symbols + media) ---------------------------------------
[[layer]]
id = "lower"
name = "Lower"
keys = """
_______  _______       _______       _______        _______        --       _______  _______  --            _______       _______       _______       _______       _______
_______  _______       _______       _______        _______        _______  _______  _______  _______       _______       _______       _______       _______       _______
_______  LSFT(KC_EQL)  LSFT(KC_MINS) LSFT(KC_9)     LSFT(KC_0)     _______  TO(0)    _______  _______       _______       _______       _______       _______       _______
KC_CAPS  LSFT(KC_1)    LSFT(KC_2)    LSFT(KC_3)     LSFT(KC_4)     LSFT(KC_5)  _______  KC_MPLY  LSFT(KC_6)  LSFT(KC_7)   LSFT(KC_8)    LSFT(KC_9)    LSFT(KC_0)    _______
_______  KC_LBRC       KC_RBRC       LSFT(KC_LBRC)  LSFT(KC_RBRC)  _______  _______  _______  _______       _______       _______       _______       _______       _______
_______  KC_HOME       KC_END        KC_MPRV        KC_MNXT        --       _______  _______  --            KC_VOLU       KC_VOLD       _______       _______       _______
"""

# --- Layer 2: Magic (system controls) ---------------------------------------
# The left thumb cluster is the connection selector. The lower arc's three
# keys are BLE slots 1-3 (USER(0)-USER(2)): tapping one selects that slot AND
# prefers BLE output, so typing moves there even while USB is plugged in. The
# upper Esc-position key (QK_OUTPUT_USB) moves typing back to USB. While
# Magic is held every key shows its own slot's state — white empty, red
# paired-but-idle, blue selected, blinking white while advertising, green
# connected — and the USB key mirrors the scheme: green carrying typing,
# blue plugged but idle, red unplugged. Magic+R toggles the maintenance lock;
# R is green while unlocked and red while locked. Bond management goes through
# `moergo-control connection`. USER(12) forwards the bootloader request to
# the right half.
#
# The lighting controls use the familiar WASD cluster plus the left/right
# arrows:
#
#   W: brightness up               S: brightness down
#   E: PaletteFX on/off            T: lighting on/off/auto
#   left arrow: previous effect    right arrow: next effect
#   C: next palette
#
# UG_OMODE, on Magic+T, cycles always-on, always-off, and plugged-in-only.
# Holding Magic temporarily wakes lighting and reveals the information view.
# The runtime scene table carries the full-layer black mask itself, so it does
# not depend on the firmware's compiled Magic scene to hide PaletteFX.
[[layer]]
id = "magic"
name = "Magic"

# The floor: Magic blanks every key it does not name, so the named binds
# below correct this whole-board KC_NO. `all` resolves through the keyboard's
# topology, so `just check` only confirms it is well formed.
[[layer.key]]
all = true
action = "KC_NO"

# F1-F5 — the same keys whose LEDs indicate layers 1-5 — latch those
# layers on/off.
[[layer.key]]
key = [0, 0]
action = "TG(1)"

[[layer.key]]
key = [0, 1]
action = "TG(2)"

[[layer.key]]
key = [0, 2]
action = "TG(3)"

[[layer.key]]
key = [0, 3]
action = "TG(4)"

[[layer.key]]
key = [0, 4]
action = "TG(5)"

[[layer.key]]
key = [0, 6]  # upper Esc-position thumb key: move typing to USB
action = "QK_OUTPUT_USB"

# The USB key's state: red = unplugged, blue = plugged but typing elsewhere,
# green = carrying typing.
[[lighting.conditional_scene]]
key = [0, 6]
color = "#b00000"
layer = { layer = 2, active = true }
connection = { usb_connected = false }

[[lighting.conditional_scene]]
key = [0, 6]
color = "#0050ff"
layer = { layer = 2, active = true }
connection = { usb_connected = true }

[[lighting.conditional_scene]]
key = [0, 6]
color = "#00c020"
layer = { layer = 2, active = true }
connection = { transport = "usb" }

[[layer.key]]
key = [2, 2]  # W: brightness up
action = "BL_UP"

[[lighting.scene]]
layer = 2
key = [2, 2]
color = "#ffff00"

[[layer.key]]
key = [2, 3]  # E: PaletteFX on/off
action = "RGB_TOG"

# E reads the state RGB_TOG flips: green while effects render, dim red while
# they are toggled off.
[[layer.key.rule]]
color = "#008000"
when = { effects = { enabled = true } }

[[layer.key.rule]]
color = "#800000"
when = { effects = { enabled = false } }

[[layer.key]]
key = [2, 4]  # R: maintenance lock toggle
action = "MAINT_LOCK_TOG"

[[layer.key]]
key = [2, 5]  # T: cycle lighting always-on / always-off / plugged-in-only
action = "UG_OMODE"

# T shows the live output-mode policy, which is what let the indicator stop
# being compiled into the firmware.
[[layer.key.rule]]
color = "#008000"
when = { output_mode = "always-on" }

[[layer.key.rule]]
color = "#800000"
when = { output_mode = "always-off" }

[[layer.key.rule]]
color = "#0040a0"
when = { output_mode = "powered-only" }

[[layer.key]]
key = [3, 0]
action = "QK_BOOT"

[[layer.key]]
key = [3, 2]  # S: brightness down
action = "BL_DOWN"

[[lighting.scene]]
layer = 2
key = [3, 2]
color = "#ffff00"

[[layer.key]]
key = [3, 6]  # lower thumb arc: BLE slot 1
action = "USER(0)"

# Slot keys share one color scheme (later rules win): white = empty, red =
# paired but idle, blue = selected or connected-but-idle, blinking white =
# advertising, green = carrying typing. Green means "keystrokes go here"
# everywhere, so a connected slot only turns green once BLE is the active
# transport — otherwise it stays blue like a plugged-but-idle USB key.
[[lighting.conditional_scene]]
key = [3, 6]
color = "#2a2a2a"
layer = { layer = 2, active = true }
connection = { bonded = { slot = 0, bonded = false } }

[[lighting.conditional_scene]]
key = [3, 6]
color = "#b00000"
layer = { layer = 2, active = true }
connection = { bonded = { slot = 0, bonded = true } }

[[lighting.conditional_scene]]
key = [3, 6]
color = "#0050ff"
layer = { layer = 2, active = true }
connection = { profile = 0, ble_state = "inactive" }

[[lighting.conditional_scene]]
key = [3, 6]
color = "#ffffff"
effect = "blink"
period_ms = 800
phase_ms = 0
duty = 50
layer = { layer = 2, active = true }
connection = { profile = 0, ble_state = "advertising" }

[[lighting.conditional_scene]]
key = [3, 6]
color = "#0050ff"
layer = { layer = 2, active = true }
connection = { profile = 0, ble_state = "connected" }

[[lighting.conditional_scene]]
key = [3, 6]
color = "#00c020"
layer = { layer = 2, active = true }
connection = { profile = 0, ble_state = "connected", transport = "ble" }

[[layer.key]]
key = [3, 13]  # forward the bootloader request to the right half
action = "USER(12)"

[[layer.key]]
key = [4, 0]
action = "QK_RBT"

[[layer.key]]
key = [4, 3]  # C: next palette
action = "RGB_HUI"

[[lighting.scene]]
layer = 2
key = [4, 3]
color = "#ff00ff"

[[layer.key]]
key = [4, 6]  # lower thumb arc: BLE slot 2
action = "USER(1)"

[[lighting.conditional_scene]]
key = [4, 6]
color = "#2a2a2a"
layer = { layer = 2, active = true }
connection = { bonded = { slot = 1, bonded = false } }

[[lighting.conditional_scene]]
key = [4, 6]
color = "#b00000"
layer = { layer = 2, active = true }
connection = { bonded = { slot = 1, bonded = true } }

[[lighting.conditional_scene]]
key = [4, 6]
color = "#0050ff"
layer = { layer = 2, active = true }
connection = { profile = 1, ble_state = "inactive" }

[[lighting.conditional_scene]]
key = [4, 6]
color = "#ffffff"
effect = "blink"
period_ms = 800
phase_ms = 0
duty = 50
layer = { layer = 2, active = true }
connection = { profile = 1, ble_state = "advertising" }

[[lighting.conditional_scene]]
key = [4, 6]
color = "#0050ff"
layer = { layer = 2, active = true }
connection = { profile = 1, ble_state = "connected" }

[[lighting.conditional_scene]]
key = [4, 6]
color = "#00c020"
layer = { layer = 2, active = true }
connection = { profile = 1, ble_state = "connected", transport = "ble" }

[[layer.key]]
key = [4, 13]  # reboot the right half
action = "QK_RBT"

[[layer.key]]
key = [5, 3]  # left arrow: previous effect
action = "RGB_RMOD"

[[lighting.scene]]
layer = 2
key = [5, 3]
color = "#ffffff"

[[layer.key]]
key = [5, 4]  # right arrow: next effect
action = "RGB_MOD"

[[lighting.scene]]
layer = 2
key = [5, 4]
color = "#ffffff"

[[layer.key]]
key = [5, 6]  # lower thumb arc: BLE slot 3
action = "USER(2)"

[[lighting.conditional_scene]]
key = [5, 6]
color = "#2a2a2a"
layer = { layer = 2, active = true }
connection = { bonded = { slot = 2, bonded = false } }

[[lighting.conditional_scene]]
key = [5, 6]
color = "#b00000"
layer = { layer = 2, active = true }
connection = { bonded = { slot = 2, bonded = true } }

[[lighting.conditional_scene]]
key = [5, 6]
color = "#0050ff"
layer = { layer = 2, active = true }
connection = { profile = 2, ble_state = "inactive" }

[[lighting.conditional_scene]]
key = [5, 6]
color = "#ffffff"
effect = "blink"
period_ms = 800
phase_ms = 0
duty = 50
layer = { layer = 2, active = true }
connection = { profile = 2, ble_state = "advertising" }

[[lighting.conditional_scene]]
key = [5, 6]
color = "#0050ff"
layer = { layer = 2, active = true }
connection = { profile = 2, ble_state = "connected" }

[[lighting.conditional_scene]]
key = [5, 6]
color = "#00c020"
layer = { layer = 2, active = true }
connection = { profile = 2, ble_state = "connected", transport = "ble" }

[[layer.key]]
key = [5, 13]
action = "TO(3)"

# --- Layer 3: Games (left-hand WASD) ----------------------------------------
# Written sparsely: no `keys` grid, so every key this layer does not name is
# transparent. Each bind's highlight scene sits beside it.
[[layer]]
id = "games"
name = "Games"

[[layer.key]]
key = [2, 2]  # W
action = "KC_W"

[[lighting.scene]]
layer = 3
key = [2, 2]
color = "#a00000"

[[layer.key]]
key = [3, 1]  # A
action = "KC_A"

[[lighting.scene]]
layer = 3
key = [3, 1]
color = "#a00000"

[[layer.key]]
key = [3, 2]  # S
action = "KC_S"

[[lighting.scene]]
layer = 3
key = [3, 2]
color = "#a00000"

[[layer.key]]
key = [3, 3]  # D
action = "KC_D"

[[lighting.scene]]
layer = 3
key = [3, 3]
color = "#a00000"

[[layer.key]]
key = [3, 6]  # left thumb: jump
action = "KC_SPC"

[[lighting.scene]]
layer = 3
key = [3, 6]
color = "#a03000"

# --- Layer 4: Mac Hyper ------------------------------------------------------
# MOD(...) directly holds the modifier combination until the key is released.
[[layer]]
id = "mac_hyper"
name = "Mac Hyper"

[[layer.key]]
key = [1, 7]  # the toggle that entered the layer leaves it
action = "TG(4)"

[[layer.key]]
key = [5, 7]  # right thumb: hold for ctrl+alt+gui
action = "MOD(MOD_LCTL|MOD_LALT|MOD_LGUI)"

# --- Layer 5: Paseo ----------------------------------------------------------
# Tap Delete normally; hold it to activate this layer. The key is TD(0), whose
# hold window lives in the morse table below.
# Mnemonics: F/A/S/L/O select Fable/Astra/Sol/Luna/Opus; Q/M/H select
# low (quick)/medium/high effort. F13-F20 are the host shortcut namespace.
#
# The host half of this contract is managed in
# `/srv/dotfiles/nix-shared/home-manager/paseo-settings-seed.nix`:
#
#   F13  command-center.shortcut:models:claude:claude-fable-5-1
#   F14  command-center.shortcut:models:codex:gpt-6-astra
#   F15  command-center.shortcut:models:codex:gpt-5.6-sol
#   F16  command-center.shortcut:models:codex:gpt-5.6-luna
#   F17  command-center.shortcut:thinking:low
#   F18  command-center.shortcut:thinking:medium
#   F19  command-center.shortcut:thinking:high
#   F20  command-center.shortcut:models:claude:claude-opus-5
#
# Rebind through Settings -> Shortcuts (Model and Thinking sections). Those rows
# only became bindable in getpaseo/paseo#2845; before that fix they rendered
# permanently greyed out because availability was computed from the live
# contribution set, which is empty on the settings route.
# Written sparsely, one bind per mnemonic with its scene beside it: Claude
# models are orange, Codex models are blue, and effort runs from green
# through amber to red.
[[layer]]
id = "paseo"
name = "Paseo"

[[layer.key]]
key = [3, 4]  # F: Fable
action = "KC_F13"

[[lighting.scene]]
layer = 5
key = [3, 4]
color = "#d06040"

[[layer.key]]
key = [3, 1]  # A: Astra
action = "KC_F14"

[[lighting.scene]]
layer = 5
key = [3, 1]
color = "#0080ff"

[[layer.key]]
key = [2, 11]  # O: Opus
action = "KC_F20"

[[lighting.scene]]
layer = 5
key = [2, 11]
color = "#d06040"

[[layer.key]]
key = [3, 2]  # S: Sol
action = "KC_F15"

[[lighting.scene]]
layer = 5
key = [3, 2]
color = "#0080ff"

[[layer.key]]
key = [3, 11]  # L: Luna
action = "KC_F16"

[[lighting.scene]]
layer = 5
key = [3, 11]
color = "#0080ff"

[[layer.key]]
key = [2, 1]  # Q: low (quick) effort
action = "KC_F17"

[[lighting.scene]]
layer = 5
key = [2, 1]
color = "#00c040"

[[layer.key]]
key = [4, 9]  # M: medium effort
action = "KC_F18"

[[lighting.scene]]
layer = 5
key = [4, 9]
color = "#d08000"

[[layer.key]]
key = [3, 8]  # H: high effort
action = "KC_F19"

[[lighting.scene]]
layer = 5
key = [3, 8]
color = "#c02020"

# --- Morse table -------------------------------------------------------------
# Morse 0 is the Paseo thumb key, addressed as TD(0) on the base layer: tap
# Delete, hold for layer 5. It exists as a morse rather than an LT so its hold
# window can be tuned here, from the host, without a firmware flash. That hold
# is a deliberate thumb press rather than a rolled home row mod, so it commits
# sooner than the firmware's 250 ms default. Everything left out — gap, quick
# tap, mode — stays at the keyboard's global defaults.
[[morse]]
name = "Paseo layer"
tap = "KC_DEL"
hold = "MO(5)"
hold_timeout_ms = 180

[lighting]
brightness = 255
output_mode = "powered-only"
wake_layers = [2]
scene_policy = "active-stack"

[lighting.background]
enabled = false
hue = 0
saturation = 0
value = 0
speed = 128
mode = "solid"

# Rynk's generic lighting-extension state. Names come from the connected
# firmware's extension descriptor; PaletteFX is the current provider.
#
# Match the Go60's default effect selection and shared rendering settings.
[lighting.effects]
effect = "Reactive"
palette = "Polarized"
value = 255
speed = 32

# Per-effect parameters, by the names the firmware advertises. Only the ones
# listed here are managed: anything left out keeps whatever the keyboard has,
# which is what makes live tuning in Rynkbench safe to leave in place.
#
# Keep Crosshair's full live tuning pinned for when it is selected by cycling.
[lighting.effects.params.Crosshair]
"Arm hue" = 0
"Arm width" = 11
Crosses = 1
"Duration x10ms" = 90
"Key hue" = 173
Motion = 0
"Pulse width" = 170

# Comet flies one body to each pressed key instead of lighting the key where
# it was pressed, so it arrives late and swings wide of a key it is redirected
# onto mid-flight. `Lag x10ms` buys a flight the full width of the board and
# `Distance pace` decides how much of that a shorter hop gives back -- at 200
# a next-key hop costs about a third of a full crossing, which is what keeps
# the comet in touch with fast typing. Every duration here is scaled by the
# shared `speed = 32` above, so the real full-board flight is nearer 440 ms.
[lighting.effects.params.Comet]
"Lag x10ms" = 32
"Distance pace" = 200
Momentum = 168
"Trail x10ms" = 20
"Head size" = 14
"Linger x10ms" = 80

# Keep Rain's tuned density available when cycling back to it. Spawn is in
# units of 10 ms, so 5 is a drop every 50 ms; the slot count then caps how many
# are airborne.
[lighting.effects.params.Rain]
Drops = 11
"Rain hue" = 172
"Spawn x10ms" = 5
Trail = 161
Width = 14

# Give Magic an opaque black floor over every otherwise-unpainted LED. This
# hides PaletteFX while preserving the explicitly colored Magic controls below
# and the higher-priority battery/output-status rules.

[[lighting.scene]]
layer = 2
key = [0, 6]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [1, 6]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [2, 6]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [3, 6]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [4, 6]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [5, 6]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [1, 5]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [3, 5]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [4, 5]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [0, 4]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [1, 4]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [2, 4]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [3, 4]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [4, 4]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [0, 3]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [1, 3]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [3, 3]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [0, 2]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [1, 2]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [4, 2]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [5, 2]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [1, 1]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [2, 1]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [3, 1]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [4, 1]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [5, 1]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [0, 0]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [1, 0]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [2, 0]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [3, 0]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [4, 0]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [5, 0]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [0, 7]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [1, 7]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [2, 7]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [3, 7]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [4, 7]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [5, 7]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [1, 8]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [2, 8]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [3, 8]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [4, 8]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [0, 9]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [1, 9]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [2, 9]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [3, 9]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [4, 9]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [5, 9]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [0, 10]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [1, 10]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [2, 10]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [3, 10]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [4, 10]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [5, 10]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [0, 11]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [1, 11]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [2, 11]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [3, 11]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [4, 11]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [5, 11]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [0, 12]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [1, 12]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [2, 12]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [3, 12]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [4, 12]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [5, 12]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [0, 13]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [1, 13]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [2, 13]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [3, 13]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [4, 13]
color = "#000000"

[[lighting.scene]]
layer = 2
key = [5, 13]
color = "#000000"

# Magic's lighting controls and state indicators live beside their
# [[layer.bind]] entries in the layer section above: brightness controls are
# yellow, effect cycling is white, and palette cycling is magenta.

# Active-layer indicators: F1-F5 show layers 1-5 in blue while active.

[[lighting.scene]]
layer = 1
key = [0, 0]
color = "#0000ff"

[[lighting.scene]]
layer = 2
key = [0, 1]
color = "#0000ff"

[[lighting.scene]]
layer = 3
key = [0, 2]
color = "#0000ff"

[[lighting.scene]]
layer = 4
key = [0, 3]
color = "#0000ff"

[[lighting.scene]]
layer = 5
key = [0, 4]
color = "#0000ff"

# Games and Paseo key highlights live beside their [[layer.bind]] entries
# above; a scene on a layer only shows while that layer is active.

# Battery gauges on the Magic layer, five LEDs per half. Order matters:
# later rules win the LEDs they share, so the level bands come first, the
# low-battery overrides next, and charging last so it beats both.

[[lighting.conditional_scene]]
key = [5, 0]
color = "#008000"
layer = { layer = 2, active = true }
battery = { node = 0, min_level = 1 }

[[lighting.conditional_scene]]
key = [4, 0]
color = "#008000"
layer = { layer = 2, active = true }
battery = { node = 0, min_level = 21 }

[[lighting.conditional_scene]]
key = [3, 0]
color = "#008000"
layer = { layer = 2, active = true }
battery = { node = 0, min_level = 41 }

[[lighting.conditional_scene]]
key = [2, 0]
color = "#008000"
layer = { layer = 2, active = true }
battery = { node = 0, min_level = 61 }

[[lighting.conditional_scene]]
key = [1, 0]
color = "#008000"
layer = { layer = 2, active = true }
battery = { node = 0, min_level = 81 }

[[lighting.conditional_scene]]
key = [5, 13]
color = "#008000"
layer = { layer = 2, active = true }
battery = { node = 1, min_level = 1 }

[[lighting.conditional_scene]]
key = [4, 13]
color = "#008000"
layer = { layer = 2, active = true }
battery = { node = 1, min_level = 21 }

[[lighting.conditional_scene]]
key = [3, 13]
color = "#008000"
layer = { layer = 2, active = true }
battery = { node = 1, min_level = 41 }

[[lighting.conditional_scene]]
key = [2, 13]
color = "#008000"
layer = { layer = 2, active = true }
battery = { node = 1, min_level = 61 }

[[lighting.conditional_scene]]
key = [1, 13]
color = "#008000"
layer = { layer = 2, active = true }
battery = { node = 1, min_level = 81 }

[[lighting.conditional_scene]]
key = [5, 0]
color = "#a03000"
layer = { layer = 2, active = true }
battery = { node = 0, min_level = 1, max_level = 40 }

[[lighting.conditional_scene]]
key = [0, 7]
color = "#a03000"
layer = { layer = 2, active = true }
battery = { node = 0, min_level = 21, max_level = 40 }

[[lighting.conditional_scene]]
key = [5, 0]
color = "#a00000"
layer = { layer = 2, active = true }
battery = { node = 0, min_level = 1, max_level = 20 }

[[lighting.conditional_scene]]
key = [5, 13]
color = "#a03000"
layer = { layer = 2, active = true }
battery = { node = 1, min_level = 1, max_level = 40 }

[[lighting.conditional_scene]]
key = [4, 13]
color = "#a03000"
layer = { layer = 2, active = true }
battery = { node = 1, min_level = 21, max_level = 40 }

[[lighting.conditional_scene]]
key = [5, 13]
color = "#a00000"
layer = { layer = 2, active = true }
battery = { node = 1, min_level = 1, max_level = 20 }

[[lighting.conditional_scene]]
key = [5, 0]
color = "#0040a0"
layer = { layer = 2, active = true }
battery = { node = 0, min_level = 1, charge = "charging" }

[[lighting.conditional_scene]]
key = [4, 0]
color = "#0040a0"
layer = { layer = 2, active = true }
battery = { node = 0, min_level = 21, charge = "charging" }

[[lighting.conditional_scene]]
key = [3, 0]
color = "#0040a0"
layer = { layer = 2, active = true }
battery = { node = 0, min_level = 41, charge = "charging" }

[[lighting.conditional_scene]]
key = [2, 0]
color = "#0040a0"
layer = { layer = 2, active = true }
battery = { node = 0, min_level = 61, charge = "charging" }

[[lighting.conditional_scene]]
key = [1, 0]
color = "#0040a0"
layer = { layer = 2, active = true }
battery = { node = 0, min_level = 81, charge = "charging" }

[[lighting.conditional_scene]]
key = [5, 13]
color = "#0040a0"
layer = { layer = 2, active = true }
battery = { node = 1, min_level = 1, charge = "charging" }

[[lighting.conditional_scene]]
key = [4, 13]
color = "#0040a0"
layer = { layer = 2, active = true }
battery = { node = 1, min_level = 21, charge = "charging" }

[[lighting.conditional_scene]]
key = [3, 13]
color = "#0040a0"
layer = { layer = 2, active = true }
battery = { node = 1, min_level = 41, charge = "charging" }

[[lighting.conditional_scene]]
key = [2, 13]
color = "#0040a0"
layer = { layer = 2, active = true }
battery = { node = 1, min_level = 61, charge = "charging" }

[[lighting.conditional_scene]]
key = [1, 13]
color = "#0040a0"
layer = { layer = 2, active = true }
battery = { node = 1, min_level = 81, charge = "charging" }

# The output-mode, effects, connection-slot, and USB-key indicators live
# beside their [[layer.bind]] entries in the Magic layer section above.
